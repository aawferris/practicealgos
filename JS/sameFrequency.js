// TALK OUT
// Taking in two positive integers
// We are checking to see if they use the same numbers (like an anagram)
// They have to have the same length / size
// They have to use the same numbers
// Negatives are immediately thrown out
// Must be O(n)

// PSEUDO CODE
// Make a function that takes two integers
// check if they are negative
// if negative return false
// if number length is not equal, return false

// create lookup helper to store num
// write a for loop to iterate through the number
// make a variable to store the index
// make a ternary that evaluates the number ? if true, +=1 : if false, = 1

// make a second for loop
// create a variable to store the index
// if the lookup (num) is false, return false
// else lookup (num) -= 1

// return true
